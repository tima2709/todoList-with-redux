import React from 'react';
import {useSelector} from "react-redux";

const Modal = () => {
    // const todoId = useSelector(state => state.todo.todo.find(todo => todo.id === editTodo))

    return (
        <div>

        </div>
    );
};

export default Modal;